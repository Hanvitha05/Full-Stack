# Server properties
server.port=8080

# Database properties (MySQL)
spring.datasource.url=jdbc:mysql://localhost:3306/smart_campus_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=hanu@1610.
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# H2 Console (Disabled for MySQL)
spring.h2.console.enabled=false
# spring.h2.console.path=/h2-console

# Hibernate properties
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Initial data properties (run data.sql after schema generation)
# spring.sql.init.mode=always
# spring.jpa.defer-datasource-initialization=true

# Thymeleaf properties
spring.thymeleaf.cache=false

# Admin credentials
admin.username=admin
admin.password=admin

# Email Settings (Update with your Gmail credentials)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hanvithabingimalla@gmail.com
spring.mail.password=etrusoydtadnruce
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
